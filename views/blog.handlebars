
<div class="container">


      <div class="card">
        <div class="card-header">
          <h2>{{blog.name}}</h2>
        </div>
        <div class="card-body">
          <p>{{blog.description}}</p>
        </div>
      </div>


</div>